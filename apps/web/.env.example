# Loaded by Docker Compose (`web` service): `apps/web/.env.${ENVIRONMENT}`
# Copy to `.env.local` or `.env.production` as needed (those files are gitignored).

ENVIRONMENT=local
PORT=3000
BACKEND_API_URL=http://api:8000
